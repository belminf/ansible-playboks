- import_playbook: linux.yml

- hosts: all
  vars:
    caddy_root: /srv/caddy
    site_name: belm.info
  roles:
    - golang
    - python
    - tmux
    - 
      role: website
      caddy_root: /srv/caddy
      site_name: belm.in
      redirects: www.belm.in, belm.info, workbench.belm.info
      tags:
        update_website
