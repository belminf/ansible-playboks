[user]
	name = {{ git_name }}
	email = {{ git_email }}
[push]
	default = simple
[pull]
	rebase = true
[alias]
	undo = reset --soft HEAD^
	st = status --short --branch
	commend = commit --amend --no-edit
